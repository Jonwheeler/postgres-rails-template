<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8" />
    <meta name="template_app" content="postgresql" />
    <title><%= page_title %></title>
    <%= stylesheet_link_tag    :application, :media => "all" %>
    <%= csrf_meta_tags %>
  </head>
  <body class="<%= body_class %>">
    <div class="container">
      <%= render 'flashes' %>
      <%= yield %>
      <%= render 'javascript' %>
    </div>
  </body>
</html>
